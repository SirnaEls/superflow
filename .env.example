# ============================================
# FlowForge Environment Variables
# ============================================

# Anthropic API Key
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Custom API endpoint
# ANTHROPIC_API_URL=https://api.anthropic.com/v1/messages

# ============================================
# Stripe Configuration
# ============================================

# Stripe Secret Key (Server-side)
# Get it from: https://dashboard.stripe.com/apikeys
# Use test keys for development (start with sk_test_)
# Use live keys for production (start with sk_live_)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Publishable Key (Client-side)
# Get it from: https://dashboard.stripe.com/apikeys
# Use test keys for development (start with pk_test_)
# Use live keys for production (start with pk_live_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Stripe Webhook Secret
# Get it from: https://dashboard.stripe.com/webhooks
# For local development, use Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
# The secret will be shown in the CLI output (starts with whsec_)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Price ID
# Create a product and price in Stripe Dashboard: https://dashboard.stripe.com/products
# Copy the Price ID (starts with price_)
NEXT_PUBLIC_STRIPE_PRICE_ID=price_your_price_id_here

# App URL (for redirects after payment)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# NextAuth Configuration
# ============================================

# NextAuth Secret
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
AUTH_SECRET=your_nextauth_secret_here
NEXTAUTH_SECRET=your_nextauth_secret_here
